# نقشه‌راه توسعه — CNC Assist

> هدف استراتژیک: رقابت سر «تعداد ماشین‌حساب» نیست. مزیت ما **AI + آنالایزر G-code** است.
> پر کردن شکاف‌های پایه (دستهٔ A) برای «کامل دیده‌شدن»، و سرمایه‌گذاری روی تمایزسازها (دستهٔ B) برای برنده‌شدن.

این نقشه بر اساس معماری فعلی نوشته شده:
`lib/features/<name>/{data,domain,presentation}` + روتینگ در [app_router.dart](lib/core/routing/app_router.dart) + Riverpod (`@riverpod` + build_runner).

---

## اصول پیاده‌سازی (برای همهٔ فازها)

- هر محاسبه‌گر جدید = یک کلاس خالص در `domain/calculators/` با تابع `static calculate(...)` (الگوی [milling_calculator.dart](lib/features/feed_speed/domain/calculators/milling_calculator.dart)).
- منطق محاسبه **هیچ‌وقت** داخل ویجت نباشد → تست‌پذیری.
- هر داده‌ی مرجع جدید = فایل JSON در `assets/data/` + ریپازیتوری با کش in-memory (الگوی `MaterialsRepository`).
- هر صفحهٔ جدید = یک `GoRoute` زیر `ShellRoute` یا child route.
- هر فیچر باید **متریک و امپریال** را پشتیبانی کند (الگوی `UnitSystem`).
- هر محاسبه‌گر یک تست واحد در `test/` داشته باشد (ورودی‌های مرزی + یک مثال شناخته‌شده).

---

## فاز ۰ — زیرساخت مشترک (پیش‌نیاز همه چیز)

قبل از افزودن ماشین‌حساب‌ها، یک‌بار این‌ها را بساز تا تکرار نکنیم:

1. **بازطراحی صفحهٔ اصلی محاسبه‌گر به یک «هاب»** — به‌جای یک محاسبه‌گر تکی، یک گرید از کارت‌های ابزار (Turning، Milling، Drilling، …). هر کارت → route مخصوص خودش.
2. **ویجت‌های ورودی مشترک**: `LabeledNumberField`, `UnitToggle`, `MaterialPicker`, `ResultCard` در `lib/core/widgets/`.
3. **`UnitSystem` را به `lib/core/`** منتقل کن (الان داخل `feed_speed/domain` است) چون همهٔ فیچرها لازمش دارند.
4. **سرویس ذخیرهٔ نتایج** را عمومی کن تا هر محاسبه‌گر بتواند در `history` ذخیره کند (الان فقط feed/speed ذخیره می‌شود).

**خروجی:** یک چارچوب که افزودن هر ماشین‌حساب جدید را به «نوشتن یک calculator + یک screen» کاهش می‌دهد.

---

## فاز ۱ — پر کردن شکاف‌های پایه (دستهٔ A)

> این‌ها در تقریباً همهٔ رقبا هستند؛ نبودشان یعنی «ناقص». بیشترین بازده نسبت به تلاش.

| # | فیچر | فایل‌ها | داده | تخمین |
|---|------|---------|------|--------|
| 1.1 | **محاسبه‌گر تراشکاری (Turning)** | `features/turning/domain/calculators/turning_calculator.dart` | از `materials.json` موجود استفاده می‌کند | متوسط |
| 1.2 | **محاسبه‌گر سوراخکاری + قلاویز (Drill & Tap)** | `features/drilling/...` | افزودن `tap_drill.json` | متوسط |
| 1.3 | **مبدل‌های سریع** (SFM↔RPM، Feed/rev↔Feed/min، تبدیل واحد) | `features/converters/...` | بدون داده | کم |
| 1.4 | **تبدیل سختی** (HRC↔HB↔HV↔Rc) | `features/converters/hardness_*` | `hardness_table.json` | کم |
| 1.5 | **MRR و Cusp Height و Chip Thinning** | افزودن به `turning`/`milling` calculators | از داده موجود | کم |

**ترتیب پیشنهادی داخل فاز ۱:** `1.1 → 1.3 → 1.4 → 1.2 → 1.5`
(اول Turning چون بیشترین تقاضا و نزدیک‌ترین به کد فعلی؛ بعد مبدل‌ها چون سریع و پربازدید.)

### جزئیات ۱.۱ (الگو برای بقیه)
- ورودی: قطر قطعه، Vc/SFM یا RPM، feed/rev، طول برش، عمق برش، تعداد پاس.
- خروجی: RPM، feed/min، زمان سیکل تراش/فیس، MRR.
- فرمول RPM تراش: متریک `(Vc×1000)/(π×D)`، امپریال `(SFM×3.82)/D` (همان فرمول فعلی — قابل استخراج به یک util مشترک `SpeedFormulas`).

---

## فاز ۲ — تمایزسازها (دستهٔ B) ⭐ هویت اپ

> اینجا برنده می‌شویم. رقبا یا ندارند یا بد پیاده کرده‌اند.

### ۲.۱ — اسکن G-code از روی عکس + آنالیز با AI ⭐⭐⭐ (قاتل بازار)
رقیب «G Code Scan» فقط OCR می‌کند. ما **هم پارسر G-code داریم هم AI** → زنجیرهٔ کامل:

```
عکس صفحهٔ ماشین → OCR (Edge Function / on-device ML Kit)
   → متن G-code → GcodeParser موجود (Haas/Sinumerik)
   → آنالیز خطا → دستیار AI توضیح می‌دهد «این خط چه می‌کند / مشکلش چیست»
```

- بازاستفاده: `GcodeParser`, `HaasParser`, `SinumerikParser` که **همین الان داریم**.
- جدید: انتخاب/گرفتن عکس (`image_picker`)، OCR (ترجیحاً Google ML Kit on-device برای آفلاین)، اتصال به Edge Function دستیار.
- ریسک: کیفیت OCR روی فونت‌های صفحهٔ CNC → نیاز به مرحلهٔ «ویرایش متن بازشناسی‌شده» قبل از آنالیز.
- تخمین: **زیاد** — اما بزرگ‌ترین تمایزساز.

### ۲.۲ — مولد مختصات Taper / Arc با جبران Nose-R ⭐⭐
کل اپ‌های «CNC Master» و «CNC Taper» فقط همین‌اند.

- `features/coordinates/domain/taper_calculator.dart` و `arc_calculator.dart` (تریگونومتری خالص).
- ورودی: نقاط شروع/پایان، زاویه/شعاع، Nose-R، جهت ابزار.
- خروجی: مختصات مطلق X/Z + هشدار تداخل (Chk NoseR) + هشدار «R بزرگ‌تر از طول مقطع».
- یک **پیش‌نمایش ۲D** ساده با `CustomPainter` ارزش ادراکی را چند برابر می‌کند.
- تخمین: **متوسط‑زیاد** (منطق تریگ سرراست، UI پیش‌نمایش زمان‌بر).

### ۲.۳ — مولدهای برنامهٔ G-code ⭐⭐
- **G76** (رزوه‌تراشی) از روی دادهٔ رزوه استاندارد.
- **چرخه‌های سوراخکاری** G81/G82/G83/G73 + الگوی سوراخ مستطیلی / دایرهٔ پیچ (bolt circle).
- خروجی قابل کپی/اشتراک‌گذاری.
- بازاستفاده: همان مدل خروجی متنی که در ۲.۱ می‌سازیم.

### ۲.۴ — True Position + مرجع GD&T
- محاسبه‌گر True Position با بونوس تلرانس (MMC/LMC).
- مرجع توضیحی نمادهای GD&T (داده‌ی استاتیک JSON، مثل `gcode_reference.json`).

---

## فاز ۳ — وفاداری و تعامل (دستهٔ C)

| فیچر | توضیح | اتکا به دارایی موجود |
|------|-------|----------------------|
| **تست/کوییز ماشینکاری** | بانک سوال + تولید پویا با AI | از دستیار AI موجود |
| **بخش سایش ابزار (Tool Wear)** | تصاویر انواع سایش + علت + راه‌حل | داده‌ی استاتیک + assets تصویری |
| **محاسبه‌گر وزن قطعه** | ۱۰ شکل × ۱۷۰ ماده (برای قیمت‌دهی) | افزودن `density` به `materials.json` |
| **جستجوی خواص مواد** | بسط `materials.json` با machinability rating | همان ریپازیتوری |

---

## ترتیب اجرای کلی (پیشنهادی)

```
فاز ۰ (زیرساخت)  ──►  فاز ۱ کامل  ──►  فاز ۲.۱ (اسکن+AI) ──►  فاز ۲.۲ (Taper/Arc)
                                            │
                                            └──►  فاز ۲.۳ → ۲.۴ → فاز ۳ (تدریجی)
```

**منطق:** اول کف بازار را پر کن (فاز ۱) تا ریویوهای «ناقص است» نگیریم؛ بعد فوراً برو سراغ تمایزسازی که فقط ما می‌توانیم بسازیم (۲.۱)، چون همان است که اپ را از ۸۲ رقیب جدا می‌کند.

---

## وضعیت پیاده‌سازی (به‌روزرسانی)

### ✅ انجام‌شده و تست‌شده (Dart-only، کامپایل و تست سبز)
- **فاز ۰**: `core/calc/units.dart` (`UnitSystem` + `SpeedFormulas`)، ویجت‌های مشترک `core/widgets/calc_widgets.dart`، رجیستری `features/tools/domain/tool_def.dart` + هاب `tools_hub_screen.dart`، روتینگ `/calculator/*`.
- **فاز ۱.۱** تراشکاری — `features/turning/` (+ تست)
- **فاز ۱.۲** مته و قلاویز — `features/drilling/` (سرعت/زمان/طول نوک + مته قلاویز)
- **فاز ۱.۳** مبدل‌ها — `features/converters/converters_screen.dart` (SFM↔RPM، fr↔fpm)
- **فاز ۱.۴** تبدیل سختی — `features/converters/hardness_*`
- **فاز ۱.۵** هلپرهای فرز — `milling_helpers.dart` (cusp, chip-thinning, MRR) — *منطق + تست؛ هنوز در UI فرز سرفیس نشده*
- **فاز ۲.۲** مختصات Taper/Arc — `features/coordinates/` با پیش‌نمایش ۲D
- **فاز ۲.۳** مولد G-code — `gcode_generator.dart` (G76 + چرخهٔ سوراخ روی دایرهٔ پیچ)
- **فاز ۲.۴** True Position + bonus tolerance — `features/precision/`
- **فاز ۳ (بخش)** وزن قطعه — `part_weight_calculator.dart` + صفحه (۷ شکل × چگالی مواد)

### 🟡 باقی‌مانده (نیازمند بک‌اند یا محتوا)
- **فاز ۲.۱ اسکن G-code از عکس** — seam آماده است: `features/gcode_analyzer/domain/gcode_ocr_service.dart`.
  مرحلهٔ بعد: ساخت Edge Function به‌نام `gcode-ocr` در Supabase که عکس را به مدل بینایی می‌دهد و متن را برمی‌گرداند؛ سپس صفحهٔ انتخاب عکس + فیلد متن قابل‌ویرایش که خروجی را به `GcodeParser` و دستیار AI می‌دهد. (کلید API فقط در Edge Function.)
- **فاز ۱.۵ سرفیس‌کردن در UI** — افزودن cusp/chip-thinning به نتایج صفحهٔ فرز (نیازمند تغییر `CutParameters` و آداپتور Hive `SavedCalculation`).
- **فاز ۳ کوییز ماشینکاری** — نیازمند بانک سوال؛ پیشنهاد: تولید پویا با دستیار AI موجود.
- **فاز ۳ سایش ابزار** — نیازمند asset‌های تصویری + متن؛ داده‌ی استاتیک JSON مثل `gcode_reference.json`.

---

## معیار «تمام‌شده» برای هر فیچر
- [ ] منطق در `domain/calculators/` با تست واحد
- [ ] پشتیبانی متریک + امپریال
- [ ] صفحه زیر `ShellRoute` با ناوبری از هاب
- [ ] قابلیت ذخیره در `history` (در صورت مرتبط بودن)
- [ ] `flutter analyze` بدون خطا و `dart format` اعمال‌شده
- [ ] رشته‌های فارسی برای نام‌ها (الگوی `name_fa`)
